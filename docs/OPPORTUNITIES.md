# 싱어송라이터 공모전 운영

이 문서는 앨범 제작 일정과 별도로, 싱어송라이터 성격의 공모전 후보를 어떻게 추적하고 웹에 반영할지 정리한다.

## 목적

- 매주 싱어송라이터 공모전을 다시 찾는다.
- 실제로 음악가 개인 프로젝트에 도움이 되는 공고만 남긴다.
- 웹에서 `수락`, `보류`, `제외`를 빠르게 판단한다.
- `수락`한 공모전은 캘린더에 넣어 마감 리마인드로 쓴다.

## 데이터 기준

- 공모전 후보는 `public.singer_songwriter_opportunities` 테이블을 기준으로 본다.
- 웹의 카드 목록은 `Supabase`에서 최신 공모전 후보를 읽는다.
- 웹에서 누르는 `수락`, `보류`, `제외`는 로그인한 사용자의 `public.opportunity_reviews`에 저장된다.
- 로그인 전에는 현재 브라우저의 임시 저장 상태를 사용한다.
- `수락`한 공모전은 웹 캘린더에 마감 일정으로 합쳐진다.

## 동기화 방식

- 로그인은 `Supabase Auth` 이메일 매직링크를 사용한다.
- 같은 이메일로 폰과 PC에서 로그인하면 공모전 판단 상태가 같은 계정으로 동기화된다.
- 웹 공개 일정은 누구나 읽을 수 있지만, 공모전 판단 쓰기는 로그인한 사용자 본인만 가능하다.
- 관리자 편집은 `public.admin_users`에 등록된 이메일만 가능하다.
- 후보 원본 갱신은 `GROUNZ` 공개 API를 우선 소스로 사용한다.

## GROUNZ 자동 수집

- 소스: `https://grounz.net/announcement?category=0`
- 실제 수집 endpoint: `https://api.grounz.net/announcement` 와 `https://api.grounz.net/announcement/:id`
- 실행 명령:

```bash
npm run opportunities:grounz
```

- 이 명령은 최근 `GROUNZ` 공고를 읽고, 싱어송라이터/자작곡/작사·작곡 성격에 맞는 항목만 골라 `public.singer_songwriter_opportunities` 테이블에 upsert한다.
- `grounz-*`로 저장된 기존 자동 수집 항목이 이번 수집 목록에서 빠지면 `closed`로 전환한다.
- 현재 수집 규칙은 다음 신호를 우선 본다.
  - `공모전`, `지원사업`, `모집 공고` 유형
  - `싱어송라이터`, `자작곡`, `작사`, `작곡`, `창작곡`, `아티스트` 키워드
  - `대관`, `전시`, `미술` 등 비목표 분야는 감점 또는 제외
- 자동 수집 결과는 시작점으로 보고, 정말 지원할 가치가 큰 공고는 웹에서 `수락/보류/제외` 판단을 한 번 더 한다.

## 관리자 모드

- 관리자 계정은 `public.admin_users` 화이트리스트로 판별한다.
- 관리자에게는 웹의 `공모전 관리자 패널`이 열리고, 여기서 공모전 추가/수정/삭제가 가능하다.
- 일반 로그인 사용자는 공모전 판단 동기화만 가능하고, 공고 원본 데이터는 수정할 수 없다.

## 상태 규칙

- `open`: 현재 접수 중이거나 마감일이 확인된 공고
- `watch`: 싱어송라이터와 잘 맞지만 아직 이번 회차 공고를 더 확인해야 하는 항목
- `closed`: 올해 모집은 종료되었지만 다음 시즌 추적 가치가 있는 항목

## 매주 월요일 업데이트 루틴

1. 공식 사이트와 공식 공고를 우선 확인한다.
2. `npm run opportunities:grounz`로 `GROUNZ` 후보를 먼저 갱신한다.
3. 싱어송라이터, 자작곡, 신인 창작 뮤지션 중심인지 다시 본다.
4. 접수 기간, 마감일, 제출 형식, 주최를 테이블에 반영한다.
5. 이미 끝난 공고는 `closed`로 바꾸고, 다시 열리면 `open`으로 복구한다.
6. 웹에서 바로 판단할 수 있게 준비물 한 줄을 남긴다.

## 현재 시드 후보 (2026-07-18 갱신)

| 상태 | 공모전 | 주최 | 마감 | 메모 |
| --- | --- | --- | --- | --- |
| open | 제4회 경산 Again 대학가요제 | 영남일보 (경산시·대구대 후원) | 2026-07-31 | 창작곡 부문 자작곡 의무. 단 대학 재학·휴학생 한정 → 참가 자격 확인 필요 |
| open | 2026 전일가요제 (VOC MUSIC FESTIVAL) | 글로벌광주방송(GGN) | 2026-09-06 | 자작곡 1곡 의무(AI 생성 금지) + 자유곡 1곡. 월별 회차제, 8·9월 회차 남음 |
| watch | EBS 헬로루키 | EBS 스페이스 공감 | — | 2026년 5~9월 월별 공모 종료. 2027 재개 대비 모니터링 |
| closed | 제37회 유재하음악경연대회 | 유재하음악장학회 (CJ문화재단) | 2026-07-03 | 접수 종료·본선 11/7 예정. 제38회는 2027년 중반 예상 |
| closed | 2026 히든스테이지 (제4회) | 뉴스핌 · 감엔터테인먼트 | 2026-04-24 | 접수 종료·본선 진행 중. 제5회는 2027년 3월경 예상 |

> 신규 2건은 `GROUNZ` 자동수집(`grounz-*`) 후보를 공식 소스로 재검증해 자작곡 성격이 맞는 것만 남긴 것이다.
> 제외한 후보: 서울음악제(협회 회원 작곡공모)·진주가인가요제(커버/헌정)·기타쇼 낙원(커버 전용 밴드)·플라이뮤지션(영상 인기투표)·대구포크스타(자작곡 비의무)·양평창작가요제(공식 공고 실존 확인 불가).
